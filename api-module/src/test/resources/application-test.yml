spring:
  config:
    activate:
      on-profile: test
  datasource:
    username: inguk
    password:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/catpaw
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
      show-sql: true
      generate-ddl: true